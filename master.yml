services:
  wireguard:
    image: linuxserver/wireguard
    container_name: wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
    volumes:
      - /home/lord/docker/wireguard/config:/config
      - /lib/modules:/lib/modules
    ports:
      - 8080:8080
      - 7878:7878
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0
      - net.ipv4.conf.all.src_valid_mark=1
    restart: unless-stopped


  qbittorrent:
    image: linuxserver/qbittorrent
    container_name: qbittorrent
    network_mode: service:wireguard
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
      - WEBUI_PORT=8080
    volumes:
      - /home/lord/docker/qbittorrent/downloads:/downloads
      - /home/lord/docker/qbittorrent/config:/config
    restart: unless-stopped

  radarr:
    image: linuxserver/radarr:latest
    container_name: radarr
    network_mode: service:wireguard
    environment:
      - PUID=0 #Cant get to work unless have root permizion  may be beacuse of exteral drive
      - PGID=0
      - TZ=America/New_York
    volumes:
      - /home/lord/docker/radarr/config:/config
      - /home/lord/media/movies:/movies
      - /home/lord/docker/qbittorrent/downloads:/downloads
    restart: unless-stopped










